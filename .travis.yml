sudo: required
language: csharp
services:
  - docker

before_install:
- docker pull lahmatbiy/vsharp.netcore2
- docker run -it -v $PWD:/VSharp:rw vsharp.netcore2 -w VSharp "dotnet restore --packages packages && dotnet build && dotnet test VSharp.Test/ --no-restore --no-build"
