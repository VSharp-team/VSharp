@model LoanExam.Models.Request
@using (Html.BeginForm())
{
    if (Model != null)
    {
        @Html.EditorForModel()
    }
    
    @Html.EditorFor(x => x.CreditInfo)
    @Html.EditorFor(x => x.PassportInfo)
    @Html.EditorFor(x => x.PersonalInfo)
    
    <div><button>Click</button></div>
}